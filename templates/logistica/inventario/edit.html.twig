{% extends 'base.html.twig' %}

{% block title %}Edit Producto{% endblock %}

{% block pageHeader %}
<div class="card-block">
    <div class="row">
        <div class="col">
            <h5 class="m-b-10">Cargar stock de producto</h5>
            <p class="text-muted m-b-10">Ingrese </p>
        </div>
        <div class="col d-flex justify-content-end">
            <a href="{{path('app_inventario_index')}}"><button type="button"
                    class="btn btn-primary waves-effect waves-light f-left d-inline-block md-trigger"
                    data-modal="modal-13">
                    <i class="fa fa-mail-reply"></i>Volver</button></a>
        </div>
    </div>
</div>
{% endblock %}


{% block pageBody %}

<div class="card">

    <div class="card-header">
        <h5>Editar</h5>
        {# <a href="{{path('app_productos_new')}}"><button type="button"
                class="btn btn-primary waves-effect waves-light f-left d-inline-block md-trigger" data-modal="modal-13">
                <i class="icofont icofont-plus m-r-5"></i> Agregar producto</button></a> #}
    </div>

    <div class="card-block">
        {# {{ include('logistica/producto/_form.html.twig') }} #}

        {{ include('logistica/inventario/_form.html.twig', {'button_label': 'Editar'}) }}
        
        {# <a href="{{ path('app_producto_index') }}">Volver</a> #}
        
        {# {{ include('logistica/producto/_delete_form.html.twig') }} #}
    </div>
</div>


{% endblock %}
